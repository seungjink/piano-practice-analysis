# Media artifacts

Default video paths used by the report viewer:

- `artifacts/take-a.mp4`
- `artifacts/take-b.mp4`

If these files exist, Markdown using `src: local:take-a` or `src: local:take-b` will load them automatically.

The file picker on the report page can still temporarily override either video in the current browser session.
